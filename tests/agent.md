## Files

test_cli.py — CLI interface tests covering help output, validation, conflicts, defaults, and dry-run behavior
test_paths.py — Path validation, target resolution, and project directory creation tests
test_planner.py — Planner and runner tests for step ordering, execution flow, config mapping, and side-effect isolation
test_uv.py — uv and git initialization step tests covering command construction and CLI integration
